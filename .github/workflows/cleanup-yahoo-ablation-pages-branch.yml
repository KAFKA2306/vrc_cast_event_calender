name: Cleanup Yahoo ablation Pages branch

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/cleanup-yahoo-ablation-pages-branch.yml

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - name: Delete completed work branch
        run: git push origin --delete feat/publish-yahoo-query-ablation || true
      - name: Remove one-time cleanup workflow
        run: |
          set -euo pipefail
          rm .github/workflows/cleanup-yahoo-ablation-pages-branch.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .github/workflows/cleanup-yahoo-ablation-pages-branch.yml
          git commit -m 'chore: remove completed Yahoo ablation Pages cleanup [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
