name: Cleanup superseded browser history branch

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/cleanup-browser-history-branch.yml

permissions:
  contents: write

concurrency:
  group: cleanup-superseded-browser-history-branch
  cancel-in-progress: false

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Delete superseded mirror branch
        run: |
          set -euo pipefail
          branch='agent/browser-history-recommendations'
          if git ls-remote --exit-code --heads origin "refs/heads/$branch" >/dev/null 2>&1; then
            git push origin --delete "$branch"
          fi
      - name: Remove one-time cleanup workflow
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          rm .github/workflows/cleanup-browser-history-branch.yml
          git add -A
          git commit -m 'chore: remove superseded recommendation branch cleanup [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
