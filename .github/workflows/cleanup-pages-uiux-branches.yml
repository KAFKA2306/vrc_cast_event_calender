name: Cleanup Pages UIUX branches

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/cleanup-pages-uiux-branches.yml

permissions:
  contents: write

concurrency:
  group: cleanup-pages-uiux-branches
  cancel-in-progress: false

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Delete implementation branches
        run: |
          set -euo pipefail
          for branch in agent/pages-uiux-deploy-trigger agent/cleanup-pages-uiux-branches; do
            if git ls-remote --exit-code --heads origin "refs/heads/$branch" >/dev/null 2>&1; then
              git push origin --delete "$branch"
            fi
          done
      - name: Remove one-time cleanup workflow
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          rm .github/workflows/cleanup-pages-uiux-branches.yml
          git add -A
          git commit -m 'chore: remove UIUX branch cleanup [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
